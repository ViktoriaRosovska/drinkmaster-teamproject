import { LogoLink, LogoIcon, LogoText } from "./Logo.styled";

const Logo = () => {
  return ( <LogoLink to="/">
       <LogoIcon />
      <LogoText>Drink&nbsp;master</LogoText>
  </LogoLink>)
};

export default Logo;
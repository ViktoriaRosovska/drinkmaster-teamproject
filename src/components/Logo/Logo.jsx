import { LogoLink, LogoIcon, LogoText } from "./Logo.styled";

const Logo = () => {
    <LogoLink to="/">
       <LogoIcon />
      <LogoText>Drink master</LogoText>
  </LogoLink>;
};

export default Logo;